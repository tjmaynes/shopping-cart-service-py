apiVersion: v1
kind: Service
metadata:
  name: python-shopping-cart-service
spec:
  selector:
    app: shopping-cart-service
  ports:
    - protocol: TCP
      port: 5000 
      name: python-shopping-cart-service 
  type: LoadBalancer

